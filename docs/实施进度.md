当前固定交付位置：output/native/Clibo.exe。当前构建通过 `npm run native:build` 导出；下文 output/release、output/floating 路径仅为历史记录。

## 0.3.2 队列纵向空间优化（2026-09-06）

移除队列页独立快捷键提示行和「粘贴顺序 / 本页加入队列」行；队首摘要单行显示，压缩搜索、分类、工具栏及底部间距。按钮悬浮说明、队首定位、恢复、下一条、合并和继续添加入口保留。历史页批量加入保留，记录行高和字体不变。

16 项前端回归测试、TypeScript / Vite 构建通过。浏览器合成数据同尺寸对比：680×560 列表 228 → 319px（约多一条记录），560×420 列表 130 → 188px；预览及操作按钮可见。此次仅调整前端布局，不改变原生回粘逻辑。

## 0.3.1 全部体验项修复（2026-09-05）

原评审第 2 部分的 6 项体验修复全部实现：搜索旧选择保护、合并保留与恢复、独立继续队列快捷键及队首提示、紧凑工具栏与预览折叠、图片缩略图/原始尺寸/复制时间、成功/手动/失败分级反馈及重试/仅复制。原有呼出快捷键保留，新增默认 Ctrl+Alt+Q，可在设置修改。

Rust 21 项、前端 16 项测试通过；TypeScript / Vite、Clippy 和 Windows Release 构建通过。详情及实机验收边界见 [0.3.1 改动与验证](0.3.1-改动与验证.md)。本条覆盖下文历史版本中“合并清空队列”“重新呼出必须再次进入排列”等旧描述。

## 0.3.0 性能与复用体验（2026-09-05）

已完成搜索误贴保护、合并保留队列、5 分钟队列恢复、图片缩略图、增量历史更新、搜索索引、虚拟列表、文字处理和命名队列。数据库升级 v4 前自动生成一致性加密备份。验证与边界见 [0.3.0 改动与验证](0.3.0-改动与验证.md)。本条覆盖下文旧版的功能限制与验证数量。

## 0.2.3 合并粘贴（2026-09-04）

排列界面新增「合并粘贴」主按钮及 Ctrl+Enter：读取完整队列的原文，按已保存顺序用 CRLF 拼接，一次写入剪贴板并回粘。目标成功收到粘贴后自动清空队列，下一次呼出从干净状态开始；目标窗口不可用时保留队列和剪贴板内容。保留原文缩进、换行和首尾空白；拒绝包含图片的混排队列，不跳过图片或部分写入。合并上限 16 MiB。逐条粘贴仍可用，合并与单条回粘共用焦点、按键释放及手动粘贴降级逻辑。

验证：15 项 Rust 测试、3 项前端测试、Clippy -D warnings 和 TypeScript / Vite 通过。新增测试覆盖重启后队列顺序、超过预览长度的完整原文、中文链接和空白、单条/空队列、图片拒绝、容量限制及队列/历史保留。浏览器 560×420 最小窗口中按钮可见，点击合并进入演示操作并保留 5 条队列；浏览器演示不写系统剪贴板。Windows Release 构建成功，已更新并后台启动 output/release/Clibo.exe，SHA256 与编译产物一致。真实目标应用接收合并内容仍待实机验收。

## 窗口交互修正（2026-09-04 晚）

标题栏改用主线程同步原生移动循环，拖动期间保护窗口；失焦后延迟 120ms 复核，窗口仍获焦、鼠标在窗口内、固定或正在拖拽时不自动隐藏。重新获焦或呼出会取消过时的失焦处理。每次呼出清空搜索和分组筛选，回到「全部」、列表顶部和第一条最新记录；分组栏横向滚动归零，保留历史、分组及待粘贴队列。本条覆盖下文旧的「当前搜索词和分类继续保留」行为。

验证：Rust 14 项测试、前端 3 项测试及 TypeScript / Vite 构建通过。Windows Release 构建通过，已同步并启动 output/release/Clibo.exe；输出文件与编译产物 SHA256 一致。Computer Use 返回「Computer Use was not approved to use clibo」，因此未完成原生鼠标拖动与快捷键完整交互复核。

# Clibo 0.1.0 实施进度

更新：2026-09-04。用户确认后按 Rust 方案开始实施；本文件描述实际代码与验证状态，完整路线图中的发布验收门槛仍保留。

## 已交付的实现

技术路线确定为 Rust + Tauri 2 + React / TypeScript + SQLite + DPAPI。Windows 系统集成采用 windows-sys；剪贴板监听是消息驱动，不轮询读取正文。读取时持有一次剪贴板锁，完成来源及隐私标记判断，并取得有限大小的快照；图片解码在释放剪贴板锁之后完成。

已实现文字/图片采集、去重与持久化、搜索筛选、收藏删除、设置清理、托盘单实例、热键呼出与回粘。前端以纯文字渲染剪贴板正文，图片仅作为本地 data URL 展示，不执行复制来的 HTML。浏览器示例与真实桌面数据分离。

已新增 680 × 560 悬浮窗：无系统标题栏、置顶、不占任务栏、拖动标题区、点击窗外收起、图钉固定。快捷键呼出时以鼠标位置及当前显示器工作区定位；固定后保留位置。搜索固定在顶部，列表和详情分别滚动，滚近底部继续加载；↑ / ↓ 与 PageUp / PageDown 选中内容后自动滚入视野。原生行为及混合 DPI 仍待实机验收。

图片按需解码/加载预览，尚无专门的持久缩略图或缩略图性能优化。生产安装器、签名、更新程序尚未交付。

## 验证记录

呼出选择重置（2026-09-04）：每次 `panel-opened` 事件都会取消旧查询结果、清除上次选择，取得新列表后选中第一条并回到顶部；当前搜索词和分类继续保留。关闭设置弹窗后的焦点恢复延后一帧，确保最终聚焦搜索框。TypeScript / Vite 和 Windows Release 构建通过，已替换并启动 `output/floating/Clibo.exe`，观察到原历史、Ctrl+Shift+V 设置及第一条高亮保留。原生窗口在后续操作前失焦隐藏，未完成「先选第二条，再重新呼出」的完整自动操作循环。

快捷键反馈排查（2026-09-04）：实际运行路径确认是 `output/floating/Clibo.exe`；界面原配置为 Ctrl+Alt+V，用户实际按 Ctrl+Shift+V。已通过原生设置界面将本机快捷键改为 Ctrl+Shift+V，观察到「设置已保存」和顶部新键位，说明系统注册及配置保存成功。验证后已取消临时固定并收起窗口。尝试从资源管理器模拟按键时，自动操作工具无法取得前台窗口进程标识，未完成隐藏状态下的模拟呼出验证。后续改善项：将启动时快捷键注册失败提示独立于底部采集状态，避免被覆盖。

| 验证 | 结果与范围 |
|---|---|
| Rust 自动测试 | 8 项通过：中文/空白、有限敏感规则、DPAPI 往返与篡改、持久化/去重/收藏/清理/原始文件无测试明文、容量过期、设置失败原子回滚、DIB 行方向与 PNG 往返/畸形和大图拒绝、悬浮窗负坐标显示器及工作区边界 |
| 前端自动测试 | 2 项通过：搜索筛选与键盘选择逻辑 |
| Rust Clippy | `-D warnings` 通过 |
| TypeScript / Vite | 类型检查与生产构建通过 |
| 浏览器界面走查 | 已核对中文单字搜索、方向键、Shift+Enter 演示复制、收藏计数、Esc 清空、设置面板及焦点；本轮补充 680×560 面板、560×420 主面板/设置面板、翻页选中及自动滚动、滚轮滚动（175px 高列表成功滚动至 240px）、图钉按钮状态；修复小高度时粘贴按钮被遮挡。仅合成示例数据，不代表原生置顶/失焦通过 |
| Windows 程序构建 | x64 Release 构建成功；最新悬浮窗版本位于 `output/floating/Clibo.exe`，旧版本保留在 `output/release/Clibo.exe` |
| 真实桌面交互 | 自动操作工具等待应用授权超时，未启动本程序；不视为启动或跨应用回粘验收通过 |

## 按原任务对应的状态

| 原任务 | 当前状态 |
|---|---|
| T01 用户需求验证 | 竞品文档已完成；5 人访谈未执行 |
| T02 技术验证 | 技术路线与核心代码完成；真实 3 应用 / 100 次回粘未执行 |
| T03 交互设计 | 可交互界面完成；5 人任务走查未执行 |
| T04–T11 核心工程 | 首轮实现完成，上表覆盖部分自动验收；系统交互、图片兼容和故障矩阵待验收 |
| T12 性能与兼容 | 未执行完整验收 |
| T13 包装与恢复 | 便携 exe 已构建；安装、签名、升级恢复待办 |
| T14 用户内测 | 未开始 |

## 下一轮顺序与退出条件

1. **真实链路验收（优先）**：在记事本、VS Code、浏览器输入框各验证文字复制、热键呼出、中文搜索、Enter 回粘和 Shift+Enter 仅复制；覆盖连续复制 100 次、快捷键冲突、输入法组词、焦点被切走、普通/管理员权限差异。逐项记录内容一致性与失败提示。
2. **图片及隐私矩阵**：截图工具、画图和浏览器图片；验证 PNG、DIB、透明度、大图拒绝、图片回粘；用合成内容验证隐私标记、排除应用和自身回写不会重复入库。
3. **可靠性与性能**：2,000 条混合记录、30 分钟压力复制、8 小时常驻、进程树总内存、首次/热呼出延迟、磁盘满和进程中断。达到原规划指标前不宣传轻量或稳定。
4. **窗口与发布**：多屏不同 DPI、窗口位置、键盘/读屏完整走查；再做安装器、卸载数据选项、签名和升级恢复演练。
5. **小范围试用**：以上门槛通过后再邀请用户，记录找回成功率、回粘失败原因与保留功能。

此轮交付是可编译运行的功能初版，不代表原规划 7–8 周的产品验证和发布任务已经全部完成。

## 0.2.0 功能更新（2026-09-04）

- 分组：创建、改名、解散；解除分组与内容元数据在同一事务更新，解散不删历史。
- 已保存：沿用原收藏标记。备注支持 1,000 字，参与搜索但不参与粘贴；重复复制保留分组、备注和已保存状态。
- 队列：文字和图片混排，逐条或当前页加入、上下排序、逐条粘贴、清空；加密持久化，排队记录不自动淘汰。无法激活目标或模拟粘贴失败时不推进，发出 Ctrl+V 后推进。目标应用实际接收仍需确认。
- 外部拖拽：UI 手柄触发主线程 OLE，IDataObject 提供 CF_UNICODETEXT，图片提供 CF_HDROP / PNG / CF_DIBV5 / CF_DIB。复制效果，不移走历史。拖拽过程中不因失焦隐藏，系统剪贴板不变。
- 拖拽 PNG 临时导出以 256 MiB 封顶；取消立即删除，成功后保留，超过 24 小时在下次启动/拖拽时清理。
- 数据库 v1 → v2 兼容迁移，旧 Secret 缺失字段默认空；旧收藏及 Ctrl+Shift+V 设置保留。旧程序拒绝打开 v2，回退需要升级前备份。
- 图片回粘兼容修正：PNG 优先，DIBV5 改为正高度自底向上，增加 24 位 CF_DIB 兼容格式，透明像素在无 Alpha 的格式中合成白底。

验证结果：13 项 Rust 测试通过（新增图片颜色/方向/透明度、OLE Unicode/文件载荷和内存交接、分组备注/队列重启去重、旧数据库升级、队列保留保护）；2 项前端测试通过；Clippy -D warnings 与 TypeScript/Vite 通过。

浏览器以独立示例数据实际验证：创建「工作话术」分组，为第一条添加「回访客户使用」，按「回访」搜索只返回该条；5 条批量入队、第一条下移、队首先使用代码片段后剩余 4 条。680×560 下粘贴和仅复制按钮可见；560×420 下主列表及详情可滚动，队列主操作可见。测试没有修改用户历史或向外部应用发送内容。

外部拖放已实现，原生数据接口测试通过，但本轮未完成飞书、Word、VS Code 等应用实际拖放/回粘验收。不能将浏览器演示的「下一条」视为跨应用粘贴通过。

发布与本机升级：0.2.0 Windows x64 Release 编译通过，已替换并后台启动 output/floating/Clibo.exe，确认进程持续运行。可执行文件 SHA256：A67652C18C4D6FA00B1C4E16D2CA96CC8E248AF2F52F38BD60C490F717A0A534。便携包：output/Clibo-0.2.0-windows-x64.zip。

升级前完整数据库文件组及旧 exe 保存在 output/backups/pre-0.2.0-20260904-151010。只读取元数据核对：数据库结构 1 → 2；升级前 54 条记录的全部 ID 与已保存状态均保留，新版运行后另采集 1 条。备份不包含在便携包中。

## 0.2.1 排列交互更新（2026-09-04）

- Alt + 点击记录：幂等加入队列并进入「排列粘贴」，已有记录不重复添加；Alt 双击不会触发直接粘贴。
- 排列模式可拖动整条记录或左侧六点手柄，显示插入线，松手才持久化顺序。拖动靠近边缘自动滚动；Escape、失焦、切换筛选、拖出列表释放取消调整。
- 「继续添加」返回历史；右侧向外箭头保留原生拖到外部应用的能力，与排序手势分开。
- 前端测试 3 项通过，包含上移/下移、筛选隐藏记录保留、目标失效和原队列不变；TypeScript / Vite 构建通过。后端数据结构未修改。
- 浏览器实际操作验证：Alt 点击代码片段进入 1 条队列；批量加入后共 5 条；整条拖动从第 1 到第 3；手柄拖动回第 1；拖出列表释放后顺序不变。未对用户历史进行测试修改，未触发外部应用粘贴。
0.2.1 Windows Release 构建通过，更新至 output/floating/Clibo.exe。SHA256：3501EE3BA305B451D565559F64F5109F935A6A6073C3F38AB2E2AA8A38A62FA6

## 0.2.2 多分组与分类栏更新（2026-09-04）

分类（全部/已保存/文字/图片）与分组标签合并为单行。超宽后显示横向滚动条，普通鼠标纵向滚轮转换为横向滚动，触控板横向滚动保留。排列粘贴及清理按钮固定在右侧。

一条记录可属于多个分组，编辑面板用复选框选择；筛选任一所属分组可找回同一记录，不重复存储正文/图片。删除分组只删除对应归属。重复复制保留全部归属及备注，原生侧校验、去重分组 ID。旧 group_id 字符串兼容读取为 group_ids 列表，数据库结构升级到 3，防止旧版写回丢失多分组。

验证：14 项 Rust 测试、3 项前端测试、Clippy 与 TypeScript/Vite 通过。新增覆盖 v2 单分组迁移、多组重启与去重、解散一组保留另一组、未知分组拒绝、取消全部归类。浏览器示例实测一条记录同时勾选「工作常用」「项目资料」，两个分组中均可找到，重开编辑两个勾选保留。680×560 下分类分组同排，分类栏可视宽 523px、内容宽 663px，滚轮后横向偏移 140px。测试未修改用户历史。
0.2.2 Release 编译通过并更新至 output/floating/Clibo.exe；SHA256：6BC1A32AD796D0700746064D43D2C29DC13E534414EF88A3A90C0738B2700F3C。升级前程序和加密数据库备份位于 output/backups/pre-0.2.2-20260904-153932。
界面简化（2026-09-04）：移除“所有分组”按钮，再次点击当前分组取消筛选；保留多分组归属及横向滚动。

输出约定（2026-09-04）：按用户要求固定 exe 到 output/release/Clibo.exe。desktop:build 成功后自动运行 desktop:export，以临时文件校验后替换，输出说明文档及 SHA256。

